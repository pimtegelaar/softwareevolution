package analyzer;

public class AnalyzeShizzle {
	
	int someUselessValue = 0;
	
	// we're going to analyze some shit
	public String doAnalyzeShizzle(String what) {
		if (what == "analyze")
			return "analyzed!";
		else
			return "this doesn't work";
	}

}
